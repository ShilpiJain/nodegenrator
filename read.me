1. Desktop need to install "sudo npm install turbo-cli -g"
2. creat a project with turbo "turbo new projectname" 
3. "npm install" (to install all the dependency on localhost)
4. "sudo chown -R $USER:$GROUP ~/.npm" (in case cache issue)
5. sudo npm install nodemon -g
6. nodemon ( will keep running server ) or turbo devserver